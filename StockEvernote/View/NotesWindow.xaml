<Window x:Class="StockEvernote.View.NotesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StockEvernote.View"
        xmlns:vm ="clr-namespace:StockEvernote.ViewModel"
        xmlns:converters="clr-namespace:StockEvernote.Converters"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:NotesViewModel}"
        WindowStartupLocation="CenterScreen"
        PreviewMouseDown="Window_PreviewMouseDown"
        Title="StockEvernote - 我的筆記" Height="600" Width="1100">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="File">
                <MenuItem Header="+ 新增筆記本" Command="{Binding AddNotebookCommand}"/>
                <Separator/>
                <MenuItem Header="Exit" Click="MenuItem_Click"/>
            </MenuItem>
        </Menu>



        <!-- ✅ ListView 要在這裡，Grid.Row="1" -->
        <ListView DockPanel.Dock="Left"
                  Width="200"
                  ItemsSource="{Binding Notebooks}"
                  SelectedItem="{Binding SelectedNotebook}"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  BorderThickness="0,0,1,0"
                  Background="#F4F4F4" >

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Focusable" Value="False"/>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=Window}}">

                        <!-- 一般顯示模式 -->
                        <TextBlock Text="{Binding Name}" FontSize="15" Padding="5,8"
                                       TextTrimming="CharacterEllipsis"
                                       Visibility="{Binding IsEditing, Converter={StaticResource InverseBoolToVisibilityConverter}}"
                                       Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=Window}}">
                                       
                            <TextBlock.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Rename"
                                                  Command="{Binding PlacementTarget.Tag.EditCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                    <MenuItem Header="Delete"
                                                  Command="{Binding PlacementTarget.Tag.DeleteCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                </ContextMenu>
                            </TextBlock.ContextMenu>
                        </TextBlock>

                        <!-- 編輯模式 -->

                        <TextBox Text="{Binding EditingName, UpdateSourceTrigger=PropertyChanged}"
                                     FontSize="15" Padding="5,8" BorderThickness="0"
                                     Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibilityConverter}}"
                                     Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=Window}}"
                                     IsVisibleChanged="RenameTextBox_IsVisibleChanged">
                                     
                            <TextBox.InputBindings>
                                <KeyBinding Key="Enter"
                                                Command="{Binding RelativeSource={RelativeSource AncestorType=TextBox}, Path=Tag.ConfirmRenameCommand}"
                                                CommandParameter="{Binding}"/>
                                <KeyBinding Key="Escape"
                                                Command="{Binding RelativeSource={RelativeSource AncestorType=TextBox}, Path=Tag.CancelRenameCommand}"
                                                CommandParameter="{Binding}"/>
                            </TextBox.InputBindings>
                        </TextBox>

                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <!-- ✅ 新增：中間筆記列表欄位，原本完全沒有這區塊 -->
        <DockPanel DockPanel.Dock="Left"
                   Width="220"
                   Background="#FAFAFA"
                   LastChildFill="True">

            <Border DockPanel.Dock="Top"
                    BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- 輸入框：IsAddingNote = true 時顯示 -->
                    <TextBox Grid.Column="0"
                             x:Name="NewNoteTextBox"
                             Text="{Binding NewNoteName, UpdateSourceTrigger=PropertyChanged}"
                             FontSize="14" Padding="10,12" BorderThickness="0"
                             Background="Transparent"
                             Visibility="{Binding IsAddingNote, Converter={StaticResource BoolToVisibilityConverter}}"
                             LostFocus="NewNoteTextBox_LostFocus">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding AddNoteCommand}"/>
                            <KeyBinding Key="Escape" Command="{Binding CancelAddNoteCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                    <!-- + New page 按鈕：IsAddingNote = false 時顯示 -->
                        <Button Grid.Column="0"
                            Content="＋  New page"
                            Command="{Binding StartAddNoteCommand}"
                            FontSize="14" Padding="10,12"
                            BorderThickness="0" Background="Transparent"
                            HorizontalContentAlignment="Left" Cursor="Hand"
                            Visibility="{Binding IsAddingNote, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                   
                </Grid>
            </Border>
            <!-- ✅ 新增：筆記列表 -->
            <ListView ItemsSource="{Binding Notes}"
                      SelectedItem="{Binding SelectedNote}"
                      BorderThickness="0" 
                      Background="Transparent"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" FontSize="14" Padding="10,8"
                                   TextTrimming="CharacterEllipsis"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </DockPanel>

        <Grid Grid.Column="1" Background="White">
            <TextBlock Text="👉 請從左側選擇或新增一本筆記本"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                     Foreground="#AAAAAA" FontSize="18"/>
        </Grid>
    </DockPanel>
</Window>